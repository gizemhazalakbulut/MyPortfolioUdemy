@model List<MyPortfolioUdemy.DAL.Entities.SocialMedia>

<ul class="home-social">
    @foreach(var item in Model)
    {
        <li>
            <a href="#"><i class="@item.Icon" aria-hidden="true"></i><span>@item.Title</span></a>
        </li>
    }
  
</ul>
